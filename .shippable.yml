# Lenguaje de la aplicación
language:
  - node_js

build:
  - pre-ci:
    - source ~/.nvm/nvm.sh && nvm install v14.13.0
    - node --version
    - npm install -g gulp-cli

  - ci:
    - gulp install
    - gulp test
